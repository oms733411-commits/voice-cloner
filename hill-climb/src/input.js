export class Input{constructor(){this.keys=new Set;this.pointers=new Map;this.gas=false;this.brake=false}bindKeyboard(){addEventListener("keydown",e=>{this.keys.add(e.code);if(["ArrowLeft","ArrowRight","ArrowUp","ArrowDown","Space"].includes(e.code))e.preventDefault()});addEventListener("keyup",e=>this.keys.delete(e.code));addEventListener("blur",()=>{this.keys.clear();this.pointers.clear();this.gas=this.brake=false})}attachButtons(g,b){this.bind(g,"gas");this.bind(b,"brake");this.bindKeyboard()}bind(el,type){const down=e=>{e.preventDefault();this.pointers.set(e.pointerId,type);this[type]=true;el.classList.add("on");if(navigator.vibrate)navigator.vibrate(8)};const up=e=>{this.pointers.delete(e.pointerId);this[type]=[...this.pointers.values()].includes(type);if(!this[type])el.classList.remove("on")};el.addEventListener("pointerdown",down);el.addEventListener("pointerup",up);el.addEventListener("pointercancel",up);el.addEventListener("pointerleave",up)}update(){this.gas=this.gas||this.keys.has("ArrowRight")||this.keys.has("KeyD")||this.keys.has("KeyW");this.brake=this.brake||this.keys.has("ArrowLeft")||this.keys.has("KeyA")||this.keys.has("KeyS");return{gas:this.gas,brake:this.brake}}}