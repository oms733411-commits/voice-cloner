export function mulberry32(seed){return()=>{let t=seed+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}export function sampleTerrain(seed,map){const r=mulberry32(seed),p=[];let x=-12,y=4.2,ph=r()*6.28;for(let i=0;i<520;i++){const wave=Math.sin(i*.19+ph)*.55+Math.sin(i*.061+ph*1.7)*.75+Math.sin(i*.013)*1.1;y+=wave*.16*map.roughness;if(i%37===0)y+=r()>.5?.45:-.3;y=Math.max(.8,Math.min(8.4,y));p.push({x,y});x+=.35}return p}export function terrainHeight(p,x){for(let i=1;i<p.length;i++)if(x<=p[i].x){const a=p[i-1],b=p[i],t=(x-a.x)/(b.x-a.x);return a.y+(b.y-a.y)*t}return p[p.length-1].y}export function buildTerrain(world,p,friction){const body=world.createBody();body.createFixture(planck.Chain(p.map(q=>({x:q.x,y:-q.y})),false),{friction,restitution:0});return body}