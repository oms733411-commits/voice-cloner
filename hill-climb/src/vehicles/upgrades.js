export const TYPES=["engine","suspension","tires","fuel"];export const price=(type,l)=>Math.round((220+l*170)*(type==="engine"?1.15:1));export function purchase(save,id,type){save.upgrades[id]??={engine:0,suspension:0,tires:0,fuel:0};const l=save.upgrades[id][type];if(l>=5)return false;const p=price(type,l);if(save.coins<p)return false;save.coins-=p;save.upgrades[id][type]++;return true}